name: ocrust

on:
  push:
    branches: [main]
    paths: ['ocrust/**', '.github/workflows/ocrust.yml']
  pull_request:
    paths: ['ocrust/**', '.github/workflows/ocrust.yml']
  workflow_dispatch:

concurrency:
  group: ocrust-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    working-directory: ocrust

jobs:
  rust:
    name: cargo test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: ocrust
      - run: cargo fmt --all --check
      - run: cargo clippy --workspace --all-targets -- -D warnings
      # The unit tests need no models and no ONNX Runtime.
      - run: cargo test -p ocrust-core --lib

  wheels:
    name: wheel (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: ubuntu-latest, target: x86_64-unknown-linux-gnu }
          - { os: ubuntu-24.04-arm, target: aarch64-unknown-linux-gnu }
          - { os: macos-latest, target: aarch64-apple-darwin }
          - { os: macos-13, target: x86_64-apple-darwin }
          - { os: windows-latest, target: x86_64-pc-windows-msvc }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: ocrust
      - name: Build wheel
        run: |
          pip install "maturin>=1.7,<2.0"
          maturin build --release --target ${{ matrix.target }} -o dist
      - name: Install and smoke-test
        run: |
          pip install --find-links dist ocrust pytest numpy
          python -c "import ocrust; print(ocrust.__version__); print(ocrust.runtime_info())"
          # Model-dependent tests skip themselves when no bundle is installed.
          pytest -q
      - uses: actions/upload-artifact@v4
        with:
          name: wheel-${{ matrix.target }}
          path: ocrust/dist/*.whl

  publish:
    name: publish to PyPI
    if: startsWith(github.ref, 'refs/tags/ocrust-v')
    needs: [rust, wheels]
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: wheel-*
          merge-multiple: true
          path: dist
      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist
